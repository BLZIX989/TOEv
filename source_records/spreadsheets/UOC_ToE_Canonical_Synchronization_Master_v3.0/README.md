# UOC_ToE_Canonical_Synchronization_Master_v3.0.xlsx (SOURCE-008)

**Deduplication note:** 73 worksheets, ALL byte-identical (verified, 0 diffs) to the corresponding
73 sheets in `source_records/spreadsheets/UOC_ToE_Master_Dependency_DAG_Closure_v1.0/` and, through
that, to SOURCE-004. This file is the exact, unmodified direct ancestor of both: DAG_Closure_v1.0 =
Sync_v3.0 + 29 new DAG_* sheets; SOURCE-004 = DAG_Closure_v1.0 + 8 new BRIDGE_* sheets. Its
`00_DASHBOARD.csv` is copied here for completeness even though it is also identical to SOURCE-004's
(the dashboard was never updated after v3.0, only a separate `DAG_00_DASHBOARD` was added later).
Full diff: `results/reconciliation/SYNCHRONIZATION_HISTORY.md`.
